#include <stdio.h>
void main()
{
    int num1, num2;
 
    printf("Enter any two numbers: ");
    scanf("%d %d", &num1, &nunm2);
    if (num1 == num2)
        printf("Both are equal\n");
    else
        printf("Both are not equal\n");
}
